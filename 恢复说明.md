# ApolloGrid 本地恢复说明

恢复日期：2026-09-09。

## 启动

双击本目录的 `start-local.cmd`。服务在后台运行，关闭启动窗口不会关闭服务。

- 前端：http://127.0.0.1:3000/
- 后台：http://127.0.0.1:3000/admin
- 后台导出：http://127.0.0.1:3000/api/admin/export

需要 Node.js 22.13 以上版本；当前验证使用 Node.js 24.16.0。初次安装可执行 `npm.cmd ci --cache .npm-cache`，前台启动可执行 `npm.cmd run dev`。

本地开发服务仅监听 127.0.0.1，并为本机请求提供开发管理员身份；非本机 Host 和跨来源请求被拒绝。生产构建不包含此开发中间件，仍使用原托管认证。

## 恢复来源与修复

源码来自移动硬盘 `J:\Codex\2026-08-24\con`。`con` 是 Windows 保留设备名，恢复目录改为 `C:\Users\guoyi\Documents\ChatGPT\apollogrid`。用长路径方式读取源文件，60 个源码、配置、资源和数据库文件全部恢复，无缺失；核对清单位于 `local-logs/recovery-manifest.json`。

重新安装 Windows 依赖，修复原 package-lock.json 与 package.json 不一致的问题；修复 Unix 专用启动命令和测试对 pnpm 的额外依赖；生成 Cloudflare 类型声明，修复类型和 lint 错误；修复缺失的橙色 CSS 变量、手动地址框被按钮挤窄、缺少 Google 数据时的建议标签。

旧 macOS node_modules、构建缓存、AppleDouble `._*` 和 `.DS_Store` 不作为运行依赖恢复；源盘未修改。原 Git 历史仍保留在移动硬盘上。

## 数据

实际网站数据库在本项目 `.wrangler/state/v3/d1/miniflare-D1DatabaseObject/` 下，启动和重启会持续使用该目录。不要删除 `.wrangler/state`。

源数据库通过 SQLite integrity_check：assessments、leads、pylon_comparisons、admins 均为 0 条，api_usage_events 为 10 条。这份备份不含历史客户及测算记录。本地启动会创建本机管理员，实际请求会继续记录 API 调用。

相邻 `Solar-测算/outputs` 是测算表输出目录；其中原始 xlsx、检查文件和临时文件与移动硬盘同名副本一致，xlsx 容器校验通过。它不是网站 D1 数据库，未把测算表伪造导入成历史客户记录。

## 尚缺的原始配置

本地和移动硬盘备份只有 `.env.example`，没有 Google Maps/Solar 和 Ideal Postcodes 密钥。将有效密钥配置在项目根目录 `.env` 中并重启：

```dotenv
GOOGLE_MAPS_API_KEY=实际密钥
IDEAL_POSTCODES_API_KEY=实际密钥
```

在密钥补齐前，邮编定位、DESNZ 用电数据、手动地址、降级测算、保存、后台、导出可用；真实 Google 屋顶影像/建筑参数和自动门牌列表尚不能复现。原云端历史记录需要原服务的数据导出，备份中不存在这些记录。

## 验证

`npm.cmd test` 执行生产构建和 4 项现有测试；`node_modules\.bin\tsc.cmd --noEmit` 检查类型；`npm.cmd run lint` 检查代码。

`scripts/browser-smoke.cjs` 使用 Playwright 和 Chrome 验证真实邮编查询、手动地址、降级测算、数据库保存、后台展示、导出、跨来源拦截及浏览器异常；截图在 `local-logs`。该脚本会创建标记为 LOCAL RESTORE CHECK 的测试测算，重复运行后需要按此标记清理测试记录。


## 2026-09-09 地址与屋顶接口复查

实测地址接口返回 configured=false；Google Building Insights 和影像接口返回 503，原因为未配置密钥。修复后增加了明确的 not_configured/provider_error/no_results 状态，避免把供应商故障伪装成没有地址；后台明确列出缺失配置。Worker 绑定与 process.env 均支持读取。修复空坐标被 Number(null) 当作 0、上游 200 但无屋顶数据时错误返回成功的问题。

本次本机搜索覆盖 Documents、Downloads、Desktop、OneDrive、D:\guoyi 和 D:\BaiduNetdiskDownload 的文件名清单（13,676 个文件）；对项目及相关文本检查 359 个文件，并检查本地项目 274 个 Git 松散对象、2026-08-10.zip 内的文本及配置、用户和系统环境变量。未找到有效的这两项密钥；旧构建 wrangler.json 的 vars 也为空。未修改源副本。原线上同名站点 API 返回 401，需要原站点身份验证。

生产构建、4 项现有测试、TypeScript 检查通过。浏览器验证了前台配置缺失提示、后台服务状态、非法坐标 400 和缺少密钥 503。尚不能声称真实地址列表或 Google 影像恢复成功。

Cloudflare 的本地秘密配置说明：https://developers.cloudflare.com/workers/vite-plugin/reference/secrets/


## API 配置已补齐

用户提供的两项密钥已写入本地 .env（不在说明中记录）。实测 NW4 1HJ 返回 5 个可选地址；选择地址后 Google Building Insights 和 TIFF 影像均返回 200，浏览器成功绘制 240×241 的真实屋顶影像及面板。以上取代前文“密钥缺失”的旧状态；历史数据恢复情况不变。
